"use strict";

var config = {
    harperdb: {
        host: "localhost",
        http_port: 9925,
        https_port: 31283
    },
    app: {
        listenPort: 8080
    }
};

module.exports = {
    config: config
};